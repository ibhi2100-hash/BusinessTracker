export declare const intelligenceRegistry: {
    inventoryMetrics: {
        initialState: () => never[];
        reduce(state: any, metric: any): any;
    }[];
    customerMetrics: {
        initialState: () => never[];
        reduce(state: any, metric: any): any;
    }[];
    profitMetrics: {
        initialState: () => never[];
        reduce(state: any, metric: any): any;
    }[];
};
