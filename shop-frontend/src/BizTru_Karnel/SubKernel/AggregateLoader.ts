import { PipelineContext, PipelineKernel } from "../contracts/SubKernelContracts";

export class AggregateLoaderKernel
implements PipelineKernel  {
    async execute(context: PipelineContext): Promise<void> {
        
    }
}