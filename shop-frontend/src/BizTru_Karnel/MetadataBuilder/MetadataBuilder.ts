import { ExecutionContext } from "../CommandFactory/ExecutionContext/ExecutionContextContract";
import { CommandMetadata, MetadataBuilderContract } from "./MetadataBuilderContract";


export class MetaBuilder implements MetadataBuilderContract {

    build(context: ExecutionContext): CommandMetadata {
        
    }
}